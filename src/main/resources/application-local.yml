spring:
  datasource:
    url: jdbc:h2:file:./.data/ledger;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: update
  kafka:
    listener:
      auto-startup: false
